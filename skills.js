// funtion to convert Celcius
// to Fahrenheit

function convertToFahrenheit(celsius) {
    return (celsius * 9/5) + 32;
}

// Driver code
let celsius = 25;
console.log(celsius + "°C is equal to " + convertToFahrenheit(celsius) + "°F");
